package site.sefaris;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Sefaris Backend — Kurumsal Web Sitesi & Yönetim Paneli REST API (rehber v4.0.0). */
@SpringBootApplication
public class SefarisApplication {
    public static void main(String[] args) {
        SpringApplication.run(SefarisApplication.class, args);
    }
}
