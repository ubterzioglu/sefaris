package site.sefaris.domain.enums;

/** Ekip üyesi rolleri (rehber bölüm 3 users.role + bölüm 5 RBAC). */
public enum Role {
    SUPER_ADMIN, ADMIN, DEVELOPER, DESIGNER, SEO_SPECIALIST, VIEWER, ACCOUNTANT
}
